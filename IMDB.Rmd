---
title: "PRATICE - IMDB 5000 Movie Dataset"
author: "sohee"
date: '2021 5 19'
output: html_document
---

#1. Library

```{r}
library(dplyr)
library(data.table)
library(reshape2)
```

#2. Data Exploration

```{r}
## Load data
#df <- read.csv("./tmdb_5000_movies.csv", header=T)
df <- read.csv("./data/movie_metadata.csv", header=T)
## Data check
str(df)
dim(df) #[1] 5043   28
## Remove Duplicates
# duplicate rows
sum(duplicated(df))
# delete duplicate rows
df <- df[!duplicated(df), ]
## Modify Movie title 
library(stringr)
df$movie_title <- gsub("혻", "", as.character(factor(df$movie_title)))
str_trim(df$movie_title, side = "right")
## Split Genres
head(df$genres)

```

```{r}

```